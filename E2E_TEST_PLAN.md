# E2Eテスト計画書

## 概要

One Night Werewolfブラウザゲームの End-to-End テスト計画。
複数ブラウザ/タブを使用したマルチプレイヤーシナリオをテストする。

## テストフレームワーク

**Playwright** を採用

選定理由:
- 複数ブラウザコンテキストの同時操作が容易
- Next.js との相性が良い
- WebSocket/Realtime のテストサポート
- 自動待機機能が優秀

## セットアップ

```bash
npm install -D @playwright/test
npx playwright install
```

## テスト環境

- **データベース**: テスト専用Supabaseプロジェクト or ローカルSupabase
- **環境変数**: `.env.test` でテスト用設定を管理
- **クリーンアップ**: 各テスト前後でデータをリセット

---

## テストシナリオ一覧

### 1. 部屋作成・参加フロー

#### TC-1.1: 部屋作成
```
前提条件: ランディングページにアクセス
手順:
  1. プレイヤー名を入力
  2. 「部屋を作成」ボタンをクリック
期待結果:
  - 部屋ページにリダイレクト
  - URLに部屋IDが含まれる
  - 自分がホストとして表示される
  - 部屋IDが表示される
```

#### TC-1.2: 部屋参加
```
前提条件: 部屋が既に作成済み
手順:
  1. 別ブラウザでランディングページにアクセス
  2. プレイヤー名を入力
  3. 部屋IDを入力
  4. 「参加」ボタンをクリック
期待結果:
  - 部屋ページにリダイレクト
  - プレイヤー一覧に自分が追加される
  - ホストのプレイヤー一覧も更新される（リアルタイム）
```

#### TC-1.3: 存在しない部屋への参加
```
手順: 無効な部屋IDで参加を試みる
期待結果: エラーメッセージが表示される
```

#### TC-1.4: 部屋からの退出
```
前提条件: 部屋に参加済み
手順: 退出ボタンをクリック
期待結果:
  - ランディングページにリダイレクト
  - 他プレイヤーの一覧から自分が削除される
```

---

### 2. ロビーフェーズ

#### TC-2.1: 役職設定（ホストのみ）
```
前提条件: ホストとして部屋に入室
手順:
  1. 役職セレクターで人狼を2枚に変更
  2. 占い師を追加
期待結果:
  - 役職カウントが更新される
  - 他プレイヤーにも変更が反映される
```

#### TC-2.2: 非ホストは役職設定不可
```
前提条件: 非ホストとして部屋に入室
期待結果: 役職設定UIが非活性または非表示
```

#### TC-2.3: ゲーム開始条件
```
シナリオA: プレイヤー不足（2人以下）
  期待結果: 「ゲーム開始」ボタンが非活性

シナリオB: 役職数が不一致
  期待結果: エラーメッセージ表示

シナリオC: 正常開始
  前提条件: 3人以上、役職数 = プレイヤー数 + 2
  期待結果: 夜フェーズに移行
```

---

### 3. 夜フェーズ

#### TC-3.1: 役職確認
```
前提条件: ゲーム開始直後
期待結果: 各プレイヤーが自分の初期役職を確認できる
```

#### TC-3.2: 人狼アクション（複数人狼）
```
前提条件: 2人の人狼プレイヤー
期待結果:
  - 互いの名前が表示される
  - 中央カード確認オプションはない
```

#### TC-3.3: 人狼アクション（単独人狼）
```
前提条件: 1人だけ人狼
期待結果:
  - 「1人狼」であることが表示される
  - 中央カード1枚を確認できる
```

#### TC-3.4: 占い師アクション
```
シナリオA: プレイヤーを占う
  手順: プレイヤーを1人選択
  期待結果: 選択したプレイヤーの役職が表示される

シナリオB: 中央カードを占う
  手順: 中央カード2枚を選択
  期待結果: 2枚の役職が表示される
```

#### TC-3.5: 怪盗アクション
```
手順: 他プレイヤーを選択して交換
期待結果:
  - 相手から奪った役職が表示される
  - 自分の最終役職が変わる
```

#### TC-3.6: トラブルメーカーアクション
```
手順: 2人のプレイヤーを選択
期待結果:
  - 交換完了メッセージが表示される
  - （最終役職は確認不可）
```

#### TC-3.7: 村人/吊人のアクション
```
前提条件: 役職が村人または吊人
期待結果:
  - アクションUIがない
  - 待機メッセージが表示される
  - 自動的に次フェーズへ進む
```

#### TC-3.8: 全員アクション完了時の自動遷移
```
前提条件: 全プレイヤーがアクション完了
期待結果: 昼フェーズに自動遷移
```

---

### 4. 昼フェーズ

#### TC-4.1: 夜情報の表示
```
前提条件: 夜アクションを実行したプレイヤー
期待結果:
  - 自分が得た情報が正しく表示される
  - 他プレイヤーの情報は見えない
```

#### TC-4.2: タイマー表示
```
期待結果: カウントダウンタイマーが動作している
```

#### TC-4.3: 投票フェーズへの遷移（ホスト操作）
```
前提条件: ホストプレイヤー
手順: 「投票フェーズへ進む」ボタンをクリック
期待結果: 全プレイヤーが投票フェーズに移行
```

---

### 5. 投票フェーズ

#### TC-5.1: 投票実行
```
手順: 任意のプレイヤーを選択して投票
期待結果:
  - 自分の投票が記録される
  - 投票済みマークが表示される
  - 他プレイヤーの画面に投票状況が反映される
```

#### TC-5.2: 投票変更
```
手順: 既に投票済みの状態で別のプレイヤーを選択
期待結果: 投票先が変更される
```

#### TC-5.3: 全員投票完了時の自動遷移
```
前提条件: 全プレイヤーが投票完了
期待結果: 結果フェーズに自動遷移
```

---

### 6. 結果フェーズ

#### TC-6.1: 処刑結果表示
```
期待結果:
  - 最多得票者が処刑として表示される
  - 処刑者の最終役職が表示される
```

#### TC-6.2: 同票処理
```
シナリオ: 2人以上が最多得票で同票
期待結果: 全員が処刑される
```

#### TC-6.3: 勝利判定 - 村人陣営勝利
```
シナリオ: 人狼が処刑された
期待結果:
  - 「村人陣営の勝利」と表示
  - 村人陣営プレイヤーに「勝利」表示
```

#### TC-6.4: 勝利判定 - 人狼陣営勝利
```
シナリオ: 人狼以外が処刑された（人狼あり）
期待結果:
  - 「人狼陣営の勝利」と表示
  - 人狼プレイヤーに「勝利」表示
```

#### TC-6.5: 勝利判定 - 吊人勝利
```
シナリオ: 吊人が処刑された
期待結果:
  - 「吊人の勝利」と表示
  - 吊人のみに「勝利」表示
```

#### TC-6.6: 勝利判定 - 平和村
```
シナリオ: 誰も人狼でない状態で無処刑
期待結果: 「村人陣営の勝利」と表示
```

#### TC-6.7: 全員の最終役職公開
```
期待結果:
  - 全プレイヤーの最終役職が表示される
  - 役職交換があった場合、変更が反映されている
  - 中央カードも表示される
```

#### TC-6.8: もう一度遊ぶ（ホスト操作）
```
手順: ホストが「もう一度遊ぶ」ボタンをクリック
期待結果:
  - 全プレイヤーがロビーフェーズに戻る
  - プレイヤー一覧は維持される
```

---

### 7. リアルタイム同期

#### TC-7.1: プレイヤー参加のリアルタイム更新
```
手順: 新規プレイヤーが部屋に参加
期待結果: 既存プレイヤーの画面が自動更新
```

#### TC-7.2: フェーズ遷移のリアルタイム同期
```
手順: ホストがフェーズを進める
期待結果: 全プレイヤーが同時に次フェーズへ移行
```

#### TC-7.3: 接続断からの復帰
```
手順:
  1. ブラウザをリロード
  2. 同じ部屋に再接続
期待結果: ゲーム状態が復元される
```

---

### 8. エッジケース・エラー処理

#### TC-8.1: ホスト離脱時の処理
```
シナリオ: ゲーム中にホストが離脱
期待結果: 次のプレイヤーがホストになる or ゲーム中断
```

#### TC-8.2: 楽観的ロック競合
```
シナリオ: 2人が同時に操作
期待結果: どちらかがリトライで成功
```

#### TC-8.3: 無効なアクション
```
シナリオ: 不正なパラメータでアクション実行
期待結果: エラーメッセージ表示、状態は変更されない
```

---

## フルゲームシナリオテスト

### FG-1: 3人プレイ基本シナリオ
```
設定:
  - プレイヤー: Alice(ホスト), Bob, Charlie
  - 役職: 人狼1, 占い師1, 村人1, (中央: 村人, 吊人)

フロー:
  1. Alice が部屋作成
  2. Bob, Charlie が参加
  3. Alice が役職を設定してゲーム開始
  4. 夜フェーズ: 各自がアクション実行
  5. 昼フェーズ: Alice が投票へ進める
  6. 投票: 全員が投票
  7. 結果: 勝敗確認
  8. Alice が「もう一度遊ぶ」

検証項目:
  - 各フェーズの表示が正しい
  - 役職配布がランダム
  - 勝敗判定が正しい
  - リアルタイム同期が動作
```

### FG-2: 5人プレイ複雑シナリオ
```
設定:
  - 役職: 人狼2, 占い師, 怪盗, トラブルメーカー, (中央: 村人, 吊人)

検証項目:
  - 怪盗/トラブルメーカーによる役職交換
  - 複数人狼の相互確認
  - 最終役職の正確な計算
```

---

## テスト実装構成

```
tests/
├── e2e/
│   ├── fixtures/
│   │   └── test-helpers.ts      # 共通ヘルパー関数
│   ├── room.spec.ts             # TC-1.x: 部屋作成・参加
│   ├── lobby.spec.ts            # TC-2.x: ロビーフェーズ
│   ├── night-phase.spec.ts      # TC-3.x: 夜フェーズ
│   ├── day-phase.spec.ts        # TC-4.x: 昼フェーズ
│   ├── voting.spec.ts           # TC-5.x: 投票フェーズ
│   ├── result.spec.ts           # TC-6.x: 結果フェーズ
│   ├── realtime.spec.ts         # TC-7.x: リアルタイム同期
│   ├── edge-cases.spec.ts       # TC-8.x: エッジケース
│   └── full-game.spec.ts        # FG-x: フルゲームシナリオ
├── playwright.config.ts
└── global-setup.ts              # テスト前のDB初期化など
```

---

## 優先度

| 優先度 | テストカテゴリ | 理由 |
|--------|--------------|------|
| P0 | FG-1, FG-2 | ゲームの基本フローが動作することを保証 |
| P1 | TC-1.x, TC-3.x, TC-6.x | コア機能 |
| P2 | TC-2.x, TC-4.x, TC-5.x | 重要なUI/UX |
| P3 | TC-7.x, TC-8.x | 信頼性・堅牢性 |

---

## 実行コマンド

```bash
# 全テスト実行
npx playwright test

# 特定ファイルのみ
npx playwright test tests/e2e/full-game.spec.ts

# UI モードで実行（デバッグ用）
npx playwright test --ui

# ヘッドフルモードで実行
npx playwright test --headed
```

---

## CI/CD 統合

```yaml
# GitHub Actions例
- name: Run E2E tests
  run: |
    npx playwright install --with-deps
    npx playwright test
  env:
    NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL }}
    NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY }}
```
